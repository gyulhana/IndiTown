import NavBottom from '../../components/NavBottom'

export default {
  title: 'Component/NavBottom',
  component: NavBottom,
  argTypes: {},
}

export const Default = (args) => {
  return <NavBottom {...args} />
}
